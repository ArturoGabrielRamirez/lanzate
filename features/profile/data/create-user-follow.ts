/* import { prisma } from "@/utils/prisma"

export async function createUserFollow(followerId: number, followingId: number) {
    return prisma.userFollow.create({
        data: {
            follower_id: followerId,
            following_id: followingId
        }
    })
} */
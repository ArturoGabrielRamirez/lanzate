import { NotificationsIcon } from "@/features/header/components"

type Props = {}

function Header({ }: Props) {
    return (
        <header>
            <NotificationsIcon />
        </header>
    )
}

export default Header